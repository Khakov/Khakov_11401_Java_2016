<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
                    http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
                    http://www.springframework.org/schema/security
                    http://www.springframework.org/schema/security/spring-security-3.2.xsd">
    <http pattern="/resource/**" security="none"/>
    <http auto-config="true" use-expressions="true">
        <csrf/>
        <intercept-url pattern="/add_my_car/**" access="isAuthenticated()"/>
        <intercept-url pattern="/document/**" access="permitAll()"/>
        <intercept-url pattern="/users/**" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/credit/**" access="permitAll()"/>
        <intercept-url pattern="/operator/**" access="hasAnyRole('ROLE_ADMIN','ROLE_OPERATOR')"/>
        <intercept-url pattern="/user/**" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/users/**" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/edit_user/**" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/edit_user/**" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/admin/**" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/profile/**" access="isAuthenticated()"/>
        <form-login
                login-page="/login"
                authentication-failure-url="/login?error=true"
                default-target-url="/"
        />
        <!-- Определяем адрес для разлогина, страницу перехода при успешном разлогине
             и указываем на необходимость очистить сессию пользователя -->
        <logout logout-url="/logout"
                logout-success-url="/login"
                invalidate-session="true"/>
        <remember-me
        key="Sem1"
        user-service-ref="customUserDetailsService"
        data-source-ref="dataSource"
        remember-me-parameter="remember-me"
        token-validity-seconds="86400"/>
    </http>

    <authentication-manager alias="authenticationManager">
        <authentication-provider user-service-ref="customUserDetailsService">
            <password-encoder hash="md5"/>
        </authentication-provider>
    </authentication-manager>

    <beans:bean id="customUserDetailsService" class="ru.kpfu.itis.khakov.service.MyUserDetailService"/>

</beans:beans>